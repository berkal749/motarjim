{
  "name": "html-native-engine",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "packages/generators/*"
  ],
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint packages/",
    "format": "prettier --write 'packages/**/*.ts'",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "eslint": "^8.57.0",
    "prettier": "^3.2.0",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  },
  "dependencies": {
    "commander": "^12.0.0",
    "parse5": "^7.1.2",
    "postcss": "^8.5.15"
  }
}

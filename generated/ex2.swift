import SwiftUI

struct GeneratedView: View {
    var body: some View {
    VStack {
        Text("Hello World HTML and CSS in a single file.")
    }
    .background(Color(.systemBackground))
    .cornerRadius(12)
    .shadow(radius: 4)
    }
}
